<UserControl x:Class="MajorMudX.UI.Views.WidgetContainterView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300" d:DesignWidth="400">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/MajorMudx.UI;component/MMXStylingResource.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <UserControl.Style>
        <Binding Source="{StaticResource MMXUCStyle}"/>
    </UserControl.Style>

    <Grid x:Name="LayoutRoot">
        <Border Style="{StaticResource MMXBorderStyle}" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button x:Name="btnSelect" Content="Activate Plugin" Style="{StaticResource MMXButtonStyle}"/>
        </Border>
    </Grid>
</UserControl>
